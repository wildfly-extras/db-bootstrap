db_bootstrap=DB Boostrap subsystem
db_bootstrap.add=Operation Adds subsystem
db_bootstrap.remove=Operation Removes subsystem
db_bootstrap.extension=Configuration for DB bootstrapper
db_bootstrap.bootstrap-deployments=Configuration for DB bootstrapper
db_bootstrap.config-bootstrap=Database bootstrap detector configuration
db_bootstrap.config-bootstrap.add=Add database bootstrap detector configuration
db_bootstrap.config-bootstrap.remove=Database bootstrap detector configuration
db_bootstrap.config-bootstrap.config=Hibernate configuration
db_bootstrap.config-bootstrap.filename=Archive file to scan
db_bootstrap.config-bootstrap.filter-on-name=Filenames to filter on when scanning the archive file internal